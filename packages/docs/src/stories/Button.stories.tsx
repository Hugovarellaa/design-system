import { Button } from '@hugo-ignite-ui/react'
import { Meta, StoryObj } from '@storybook/react'

export default {
  title: 'Button',
  component: Button,
  args: {},
} as Meta

export const Primary: StoryObj = {}

export const Secondary: StoryObj = {}
